import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        main: '#5086A8',
      },
      secondary: {
        main: '#00C0BB',
      }
    },
  });

  export default theme;